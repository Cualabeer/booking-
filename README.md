# Booking App (Postgres)

A minimal booking app with Node.js + Express + Postgres. Open admin view at `/bookings`.

## Local run
Create a `.env` with DATABASE_URL (e.g. a local Postgres or a Render DSN):
# booking-